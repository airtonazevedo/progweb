<div id="ranking" class="container mt-4">
    <div class="row">
      <div class="col-sm">
        <h2>Ranking dos jogadores</h2>
      </div>
    </div>
    <div class="row">
      <div class="col-sm">
        <table class="table table-hover">
          <thead>
            <tr>
              <th scope="col">Nome</th>
              <th scope="col">Pontuacao</th>
              <th scope="col">Data/Hora</th>
            </tr>
          </thead>
          <tbody>
            <% jogadas.forEach(function(jogada) { %>
            <tr>
              <th scope="row"><%= jogada.nome %></th>
              <td><%= jogada.pontuacao %></td>
              <td><%= jogada.data %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>